=== FILE: ./admin/config.js ===
=== FILE: ./admin.html ===
<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<title>Admin Stovensa</title>

<style>
* {
    box-sizing: border-box;
}

body {
    margin: 0;
    font-family: Arial, sans-serif;
    background: #f4f7fb;
    color: #172033;
}

header {
    background: linear-gradient(135deg, #1769ff, #7c3aed);
    color: white;
    padding: 28px 20px;
    text-align: center;
}

header h1 {
    margin: 0 0 8px;
}

.container {
    max-width: 850px;
    margin: auto;
    padding: 20px;
}

.card {
    background: white;
    padding: 22px;
    margin-bottom: 20px;
    border-radius: 18px;
    box-shadow: 0 8px 30px rgba(0,0,0,.08);
}

.card h2 {
    margin-top: 0;
    color: #1769ff;
}

label {
    display: block;
    font-weight: bold;
    margin: 15px 0 7px;
}

input,
textarea {
    width: 100%;
    padding: 13px;
    border: 1px solid #d5dbe5;
    border-radius: 10px;
    font-size: 16px;
    font-family: inherit;
}

textarea {
    min-height: 130px;
    resize: vertical;
}

input[type="file"] {
    background: #f8fafc;
}

button {
    width: 100%;
    padding: 15px;
    border: 0;
    border-radius: 12px;
    background: #1769ff;
    color: white;
    font-size: 17px;
    font-weight: bold;
    cursor: pointer;
}

button:hover {
    background: #0d55d9;
}

.preview {
    display: none;
    margin-top: 15px;
    padding: 10px;
    background: #f1f5f9;
    border-radius: 12px;
}

.preview img,
.preview video {
    display: block;
    width: 100%;
    max-height: 400px;
    object-fit: contain;
    border-radius: 10px;
}

#status {
    display: none;
    margin-top: 15px;
    padding: 13px;
    border-radius: 10px;
    background: #eef4ff;
}

.warning {
    font-size: 14px;
    color: #687386;
    line-height: 1.6;
}

@media (max-width: 600px) {
    .container {
        padding: 12px;
    }

    .card {
        padding: 18px;
    }
}
</style>
</head>

<body>

<header>
    <h1>⚙️ Admin Stovensa</h1>
    <p>Kelola isi website Stovensa</p>
</header>

<div class="container">

    <!-- JUDUL -->

    <div class="card">

        <h2>✏️ Judul Website</h2>

        <label for="judul">
            Judul utama
        </label>

        <input
            id="judul"
            type="text"
            placeholder="Contoh: STOVENSA">

    </div>


    <!-- DESKRIPSI -->

    <div class="card">

        <h2>📝 Deskripsi</h2>

        <label for="deskripsi">
            Deskripsi utama
        </label>

        <textarea
            id="deskripsi"
            placeholder="Tulis deskripsi Stovensa..."></textarea>

    </div>


    <!-- BERITA -->

    <div class="card">

        <h2>📰 Berita Terbaru</h2>

        <label for="beritaJudul">
            Judul berita
        </label>

        <input
            id="beritaJudul"
            type="text"
            placeholder="Judul berita">

        <label for="beritaIsi">
            Isi berita
        </label>

        <textarea
            id="beritaIsi"
            placeholder="Tuliskan berita..."></textarea>

    </div>


    <!-- FOTO -->

    <div class="card">

        <h2>📸 Foto</h2>

        <label for="foto">
            Pilih foto
        </label>

        <input
            id="foto"
            type="file"
            accept="image/*">

        <div
            class="preview"
            id="fotoPreview">

            <img
                id="fotoImage"
                alt="Preview foto">

        </div>

    </div>


    <!-- VIDEO -->

    <div class="card">

        <h2>🎬 Video</h2>

        <label for="video">
            Pilih video
        </label>

        <input
            id="video"
            type="file"
            accept="video/*">

        <div
            class="preview"
            id="videoPreview">

            <video
                id="videoPlayer"
                controls>
            </video>

        </div>

    </div>


    <!-- SIMPAN -->

    <div class="card">

        <button id="saveButton">
            💾 SIMPAN PERUBAHAN
        </button>

        <div id="status"></div>

        <p class="warning">
            Perubahan akan dikirim melalui server Termux
            kemudian diperbarui ke repository GitHub.
        </p>

    </div>

</div>


<script>

/* =========================
   PREVIEW FOTO
========================= */

const foto =
    document.getElementById("foto");

foto.addEventListener("change", function () {

    const file = this.files[0];

    if (!file) return;

    const url =
        URL.createObjectURL(file);

    document.getElementById(
        "fotoImage"
    ).src = url;

    document.getElementById(
        "fotoPreview"
    ).style.display = "block";
});


/* =========================
   PREVIEW VIDEO
========================= */

const video =
    document.getElementById("video");

video.addEventListener("change", function () {

    const file = this.files[0];

    if (!file) return;

    const url =
        URL.createObjectURL(file);

    document.getElementById(
        "videoPlayer"
    ).src = url;

    document.getElementById(
        "videoPreview"
    ).style.display = "block";
});


/* =========================
   SIMPAN
========================= */

document
.getElementById("saveButton")
.addEventListener("click", async function () {

    const status =
        document.getElementById("status");

    const judul =
        document.getElementById("judul").value;

    const deskripsi =
        document.getElementById("deskripsi").value;

    const beritaJudul =
        document.getElementById("beritaJudul").value;

    const beritaIsi =
        document.getElementById("beritaIsi").value;


    status.style.display = "block";

    status.textContent =
        "⏳ Mengirim perubahan...";


    try {

        const response =
            await fetch("/save", {

                method: "POST",

                headers: {
                    "Content-Type":
                        "application/json"
                },

                body: JSON.stringify({

                    judul: judul,

                    deskripsi: deskripsi,

                    beritaJudul:
                        beritaJudul,

                    beritaIsi:
                        beritaIsi

                })

            });


        const result =
            await response.json();


        if (result.success) {

            status.textContent =
                "✅ Perubahan berhasil dikirim ke GitHub.";

        } else {

            status.textContent =
                "❌ " +
                (result.message ||
                "Gagal menyimpan.");

            console.error(
                result.error
            );
        }


    } catch (error) {

        console.error(error);

        status.textContent =
            "❌ Server Termux tidak dapat dihubungi.";
    }

});

</script>

</body>
</html>
=== FILE: ./index.html ===
<!DOCTYPE html>
<html lang="id">

<head>

<meta charset="UTF-8">

<meta
name="viewport"
content="width=device-width,initial-scale=1.0">

<title>Admin Stovensa</title>

<style>

*{
    box-sizing:border-box;
}

body{
    margin:0;
    font-family:Arial,sans-serif;
    background:#f3f6fb;
    color:#172033;
}

header{
    background:linear-gradient(
        135deg,
        #1769ff,
        #7c3aed
    );
    color:white;
    padding:30px 20px;
    text-align:center;
}

.container{
    max-width:900px;
    margin:auto;
    padding:20px;
}

.card{
    background:white;
    border-radius:18px;
    padding:22px;
    margin-bottom:20px;
    box-shadow:0 8px 30px rgba(0,0,0,.08);
}

h2{
    margin-top:0;
    color:#1769ff;
}

label{
    display:block;
    font-weight:bold;
    margin:15px 0 7px;
}

input,
textarea{
    width:100%;
    padding:13px;
    border:1px solid #d6dce6;
    border-radius:10px;
    font-size:16px;
    font-family:inherit;
}

textarea{
    min-height:120px;
    resize:vertical;
}

button{
    border:0;
    border-radius:10px;
    padding:13px 18px;
    font-size:15px;
    font-weight:bold;
    cursor:pointer;
}

.primary{
    width:100%;
    background:#1769ff;
    color:white;
}

.add{
    margin-top:15px;
    background:#16a34a;
    color:white;
}

.delete{
    background:#dc2626;
    color:white;
    margin-top:10px;
}

.preview{
    display:none;
    margin-top:15px;
}

.preview img,
.preview video{
    width:100%;
    max-height:400px;
    object-fit:contain;
    border-radius:12px;
}

.item{
    background:#f8fafc;
    border:1px solid #e2e8f0;
    border-radius:12px;
    padding:15px;
    margin-top:12px;
}

.item img,
.item video{
    width:100%;
    max-height:250px;
    object-fit:cover;
    border-radius:10px;
    margin-top:10px;
}

.status{
    display:none;
    margin-top:15px;
    padding:13px;
    border-radius:10px;
    background:#eef4ff;
}

.small{
    color:#64748b;
    font-size:14px;
    line-height:1.6;
}

</style>

</head>

<body>

<header>

<h1>⚙️ Admin Stovensa</h1>

<p>
Panel kontrol website
</p>

</header>


<div class="container">


<!-- WEBSITE -->

<div class="card">

<h2>🌐 Website</h2>

<label>
Judul utama
</label>

<input
id="judul"
placeholder="STOVENSA">

<label>
Deskripsi
</label>

<textarea
id="deskripsi"
placeholder="Deskripsi website">
</textarea>

<button
class="primary"
onclick="saveWebsite()">

💾 Simpan Website

</button>

</div>


<!-- BERITA -->

<div class="card">

<h2>📰 Berita</h2>

<label>
Judul berita
</label>

<input
id="beritaJudul"
placeholder="Judul berita">

<label>
Isi berita
</label>

<textarea
id="beritaIsi"
placeholder="Isi berita">
</textarea>

<button
class="add"
onclick="addNews()">

➕ Tambah Berita

</button>

<div id="beritaList"></div>

</div>


<!-- FOTO -->

<div class="card">

<h2>📸 Foto</h2>

<input
id="foto"
type="file"
accept="image/*">

<div
id="fotoPreview"
class="preview">

<img
id="fotoPreviewImage">

</div>

<button
class="add"
onclick="uploadPhoto()">

📤 Upload Foto

</button>

<div id="fotoList"></div>

</div>


<!-- VIDEO -->

<div class="card">

<h2>🎬 Video</h2>

<input
id="video"
type="file"
accept="video/*">

<div
id="videoPreview"
class="preview">

<video
id="videoPreviewPlayer"
controls>
</video>

</div>

<button
class="add"
onclick="uploadVideo()">

📤 Upload Video

</button>

<div id="videoList"></div>

<p class="small">

Gunakan video berukuran wajar.
Video besar membutuhkan waktu upload
lebih lama dan dapat ditolak oleh GitHub.

</p>

</div>


<!-- STATUS -->

<div class="card">

<h2>📡 Status</h2>

<div
id="status"
class="status">
Menunggu...
</div>

</div>


</div>


<script>

let siteData = null;


/* LOAD DATA */

async function loadData(){

    try{

        const response =
            await fetch("/api/data");

        siteData =
            await response.json();

        document.getElementById(
            "judul"
        ).value =
            siteData.judul || "";

        document.getElementById(
            "deskripsi"
        ).value =
            siteData.deskripsi || "";

        renderLists();

    }catch(error){

        showStatus(
            "❌ Gagal mengambil data.",
            true
        );

        console.error(error);

    }

}


/* STATUS */

function showStatus(
    text,
    error=false
){

    const status =
        document.getElementById(
            "status"
        );

    status.style.display =
        "block";

    status.textContent =
        text;

}


/* KIRIM */

async function send(data){

    showStatus(
        "⏳ Menyimpan..."
    );

    try{

        const response =
            await fetch(
                "/api/save",
                {
                    method:"POST",

                    headers:{
                        "Content-Type":
                            "application/json"
                    },

                    body:
                        JSON.stringify(data)
                }
            );

        const result =
            await response.json();

        if(!result.success){

            throw new Error(
                result.message
            );

        }

        siteData =
            result.data;

        renderLists();

        showStatus(
            "✅ Berhasil disimpan ke GitHub."
        );

    }catch(error){

        console.error(error);

        showStatus(
            "❌ " +
            error.message,
            true
        );

    }

}


/* WEBSITE */

function saveWebsite(){

    send({

        aksi:"website",

        judul:
            document.getElementById(
                "judul"
            ).value,

        deskripsi:
            document.getElementById(
                "deskripsi"
            ).value

    });

}


/* BERITA */

function addNews(){

    const judul =
        document.getElementById(
            "beritaJudul"
        ).value;

    const isi =
        document.getElementById(
            "beritaIsi"
        ).value;

    if(!judul || !isi){

        alert(
            "Isi judul dan berita terlebih dahulu."
        );

        return;
    }

    send({

        aksi:"tambah_berita",

        beritaJudul:judul,

        beritaIsi:isi

    });

    document.getElementById(
        "beritaJudul"
    ).value="";

    document.getElementById(
        "beritaIsi"
    ).value="";

}


/* FOTO PREVIEW */

document.getElementById(
    "foto"
).addEventListener(
    "change",
    function(){

        const file =
            this.files[0];

        if(!file) return;

        const url =
            URL.createObjectURL(file);

        document.getElementById(
            "fotoPreviewImage"
        ).src = url;

        document.getElementById(
            "fotoPreview"
        ).style.display =
            "block";

    }
);


/* VIDEO PREVIEW */

document.getElementById(
    "video"
).addEventListener(
    "change",
    function(){

        const file =
            this.files[0];

        if(!file) return;

        const url =
            URL.createObjectURL(file);

        document.getElementById(
            "videoPreviewPlayer"
        ).src = url;

        document.getElementById(
            "videoPreview"
        ).style.display =
            "block";

    }
);


/* UPLOAD FOTO */

async function uploadPhoto(){

    const input =
        document.getElementById(
            "foto"
        );

    const file =
        input.files[0];

    if(!file){

        alert(
            "Pilih foto terlebih dahulu."
        );

        return;
    }

    const base64 =
        await fileToBase64(file);

    await send({

        aksi:"upload_media",

        type:"image",

        nama:file.name,

        data:base64

    });

    input.value="";

}


/* UPLOAD VIDEO */

async function uploadVideo(){

    const input =
        document.getElementById(
            "video"
        );

    const file =
        input.files[0];

    if(!file){

        alert(
            "Pilih video terlebih dahulu."
        );

        return;
    }

    if(
        file.size >
        50 * 1024 * 1024
    ){

        alert(
            "Video terlalu besar. Gunakan video di bawah 50 MB."
        );

        return;
    }

    const base64 =
        await fileToBase64(file);

    await send({

        aksi:"upload_media",

        type:"video",

        nama:file.name,

        data:base64

    });

    input.value="";

}


/* FILE KE BASE64 */

function fileToBase64(file){

    return new Promise(
        (resolve,reject)=>{

            const reader =
                new FileReader();

            reader.onload =
                () => resolve(
                    reader.result
                );

            reader.onerror =
                reject;

            reader.readAsDataURL(
                file
            );

        }
    );

}


/* RENDER */

function renderLists(){

    if(!siteData) return;


    const berita =
        document.getElementById(
            "beritaList"
        );

    berita.innerHTML="";

    siteData.berita.forEach(
        item=>{

            berita.innerHTML += `

            <div class="item">

                <strong>
                    ${escapeHTML(item.judul)}
                </strong>

                <p>
                    ${escapeHTML(item.isi)}
                </p>

                <button
                    class="delete"
                    onclick="deleteItem(
                        'hapus_berita',
                        '${item.id}'
                    )">

                    🗑️ Hapus

                </button>

            </div>

            `;

        }
    );


    const foto =
        document.getElementById(
            "fotoList"
        );

    foto.innerHTML="";

    siteData.foto.forEach(
        item=>{

            foto.innerHTML += `

            <div class="item">

                <strong>
                    ${escapeHTML(item.nama)}
                </strong>

                <img
                    src="${item.url}">

                <button
                    class="delete"
                    onclick="deleteItem(
                        'hapus_foto',
                        '${item.id}'
                    )">

                    🗑️ Hapus Foto

                </button>

            </div>

            `;

        }
    );


    const video =
        document.getElementById(
            "videoList"
        );

    video.innerHTML="";

    siteData.video.forEach(
        item=>{

            video.innerHTML += `

            <div class="item">

                <strong>
                    ${escapeHTML(item.nama)}
                </strong>

                <video
                    controls
                    src="${item.url}">
                </video>

                <button
                    class="delete"
                    onclick="deleteItem(
                        'hapus_video',
                        '${item.id}'
                    )">

                    🗑️ Hapus Video

                </button>

            </div>

            `;

        }
    );

}


/* DELETE */

function deleteItem(
    aksi,
    id
){

    if(
        !confirm(
            "Yakin ingin menghapus?"
        )
    ){

        return;

    }

    send({

        aksi:aksi,

        id:id

    });

}


/* ESCAPE */

function escapeHTML(text){

    return String(text || "")
        .replace(/&/g,"&amp;")
        .replace(/</g,"&lt;")
        .replace(/>/g,"&gt;")
        .replace(/"/g,"&quot;")
        .replace(/'/g,"&#039;");

}


loadData();

</script>

</body>
</html>
=== FILE: ./server.js ===
const http = require("http");
const fs = require("fs");
const path = require("path");
const { execFile } = require("child_process");

const PORT = 3000;
const ROOT = __dirname;

const DATA_DIR =
    path.join(ROOT, "data");

const MEDIA_DIR =
    path.join(ROOT, "media");

const DATA_FILE =
    path.join(
        DATA_DIR,
        "site.json"
    );

const INDEX_FILE =
    path.join(
        ROOT,
        "index.html"
    );


fs.mkdirSync(
    DATA_DIR,
    { recursive:true }
);

fs.mkdirSync(
    MEDIA_DIR,
    { recursive:true }
);


function defaultData(){

    return {

        judul:"STOVENSA",

        deskripsi:
            "Website informasi dan kegiatan Stovensa.",

        berita:[],

        foto:[],

        video:[]

    };

}


function loadData(){

    if(
        !fs.existsSync(
            DATA_FILE
        )
    ){

        return defaultData();

    }

    try{

        return JSON.parse(
            fs.readFileSync(
                DATA_FILE,
                "utf8"
            )
        );

    }catch{

        return defaultData();

    }

}


function saveData(data){

    fs.writeFileSync(

        DATA_FILE,

        JSON.stringify(
            data,
            null,
            2
        ),

        "utf8"

    );

}


function escapeHTML(text){

    return String(text || "")
        .replace(/&/g,"&amp;")
        .replace(/</g,"&lt;")
        .replace(/>/g,"&gt;")
        .replace(/"/g,"&quot;")
        .replace(/'/g,"&#039;");

}


function createWebsite(data){

    let html =
        fs.readFileSync(
            INDEX_FILE,
            "utf8"
        );


    html =
        html.replace(
            /(<title>)[\s\S]*?(<\/title>)/i,
            `$1${escapeHTML(
                data.judul
            )}$2`
        );


    html =
        html.replace(
            /(<h1[^>]*>)[\s\S]*?(<\/h1>)/i,
            `$1${escapeHTML(
                data.judul
            )}$2`
        );


    html =
        html.replace(
            /(<p id="deskripsi">)[\s\S]*?(<\/p>)/i,
            `$1${escapeHTML(
                data.deskripsi
            )}$2`
        );


    fs.writeFileSync(

        INDEX_FILE,

        html,

        "utf8"

    );

}


function runGit(args){

    return new Promise(
        (resolve,reject)=>{

            execFile(

                "git",

                args,

                {
                    cwd:ROOT
                },

                (
                    error,
                    stdout,
                    stderr
                )=>{

                    if(error){

                        reject(
                            stderr ||
                            stdout ||
                            error.message
                        );

                        return;

                    }

                    resolve(stdout);

                }

            );

        }
    );

}


async function pushGitHub(){

    await runGit([
        "add",
        "index.html",
        "data",
        "media"
    ]);


    try{

        await runGit([

            "commit",

            "-m",

            "Update Stovensa dari Admin"

        ]);

    }catch(error){

        const message =
            String(error);

        if(
            !message.includes(
                "nothing to commit"
            )
        ){

            throw error;

        }

    }


    await runGit([

        "push",

        "origin",

        "main"

    ]);

}


function send(
    res,
    status,
    type,
    data
){

    res.writeHead(
        status,
        {
            "Content-Type":
                type +
                "; charset=utf-8"
        }
    );

    res.end(data);

}


function sendJSON(
    res,
    status,
    data
){

    send(

        res,

        status,

        "application/json",

        JSON.stringify(data)

    );

}


function receiveBody(req){

    return new Promise(
        (resolve,reject)=>{

            let body="";

            req.on(
                "data",
                chunk=>{

                    body += chunk;

                    if(
                        body.length >
                        70 * 1024 * 1024
                    ){

                        reject(
                            new Error(
                                "Data terlalu besar."
                            )
                        );

                        req.destroy();

                    }

                }
            );


            req.on(
                "end",
                ()=>{

                    try{

                        resolve(
                            JSON.parse(
                                body
                            )
                        );

                    }catch{

                        reject(
                            new Error(
                                "Data tidak valid."
                            )
                        );

                    }

                }
            );


            req.on(
                "error",
                reject
            );

        }
    );

}


const server =
http.createServer(
async(req,res)=>{


    /*
       ADMIN
    */

    if(
        req.method === "GET" &&
        req.url === "/"
    ){

        return send(

            res,

            200,

            "text/html",

            fs.readFileSync(

                path.join(
                    ROOT,
                    "admin.html"
                )

            )

        );

    }


    /*
       DATA
    */

    if(
        req.method === "GET" &&
        req.url === "/api/data"
    ){

        return sendJSON(

            res,

            200,

            loadData()

        );

    }


    /*
       SIMPAN
    */

    if(
        req.method === "POST" &&
        req.url === "/api/save"
    ){

        try{

            const input =
                await receiveBody(
                    req
                );

            const data =
                loadData();


            /*
               WEBSITE
            */

            if(
                input.aksi ===
                "website"
            ){

                data.judul =
                    String(
                        input.judul ||
                        ""
                    );

                data.deskripsi =
                    String(
                        input.deskripsi ||
                        ""
                    );

            }


            /*
               BERITA
            */

            if(
                input.aksi ===
                "tambah_berita"
            ){

                data.berita.unshift({

                    id:Date.now(),

                    judul:
                        String(
                            input.beritaJudul ||
                            ""
                        ),

                    isi:
                        String(
                            input.beritaIsi ||
                            ""
                        )

                });

            }


            if(
                input.aksi ===
                "hapus_berita"
            ){

                data.berita =
                    data.berita.filter(
                        item=>
                            String(
                                item.id
                            ) !==
                            String(
                                input.id
                            )
                    );

            }


            /*
               MEDIA
            */

            if(
                input.aksi ===
                "upload_media"
            ){

                const isVideo =
                    input.type ===
                    "video";


                const base64 =
                    String(
                        input.data ||
                        ""
                    );


                if(
                    !base64
                ){

                    throw new Error(
                        "File kosong."
                    );

                }


                const extension =
                    isVideo
                        ? ".mp4"
                        : ".jpg";


                const filename =
                    Date.now() +
                    "_" +
                    Math.random()
                        .toString(36)
                        .substring(2,8) +
                    extension;


                const relative =
                    "media/" +
                    filename;


                const output =
                    path.join(
                        ROOT,
                        relative
                    );


                const clean =
                    base64.includes(",")
                        ? base64.split(",")[1]
                        : base64;


                fs.writeFileSync(

                    output,

                    Buffer.from(
                        clean,
                        "base64"
                    )

                );


                const item = {

                    id:Date.now(),

                    nama:
                        String(
                            input.nama ||
                            filename
                        ),

                    url:
                        relative

                };


                if(isVideo){

                    data.video.unshift(
                        item
                    );

                }else{

                    data.foto.unshift(
                        item
                    );

                }

            }


            /*
               HAPUS FOTO
            */

            if(
                input.aksi ===
                "hapus_foto"
            ){

                const item =
                    data.foto.find(
                        x=>
                            String(x.id) ===
                            String(input.id)
                    );


                if(item){

                    const file =
                        path.join(
                            ROOT,
                            item.url
                        );

                    if(
                        fs.existsSync(
                            file
                        )
                    ){

                        fs.unlinkSync(
                            file
                        );

                    }

                }


                data.foto =
                    data.foto.filter(
                        x=>
                            String(x.id) !==
                            String(input.id)
                    );

            }


            /*
               HAPUS VIDEO
            */

            if(
                input.aksi ===
                "hapus_video"
            ){

                const item =
                    data.video.find(
                        x=>
                            String(x.id) ===
                            String(input.id)
                    );


                if(item){

                    const file =
                        path.join(
                            ROOT,
                            item.url
                        );

                    if(
                        fs.existsSync(
                            file
                        )
                    ){

                        fs.unlinkSync(
                            file
                        );

                    }

                }


                data.video =
                    data.video.filter(
                        x=>
                            String(x.id) !==
                            String(input.id)
                    );

            }


            /*
               SIMPAN
            */

            saveData(data);


            /*
               UPDATE WEBSITE
            */

            createWebsite(
                data
            );


            /*
               GITHUB
            */

            await pushGitHub();


            return sendJSON(

                res,

                200,

                {

                    success:true,

                    message:
                        "Berhasil disimpan ke GitHub.",

                    data:data

                }

            );


        }catch(error){

            console.error(
                error
            );

            return sendJSON(

                res,

                500,

                {

                    success:false,

                    message:
                        String(
                            error
                        )

                }

            );

        }

    }


    /*
       FILE MEDIA
    */

    if(
        req.method === "GET"
    ){

        const requested =
            decodeURIComponent(
                req.url.split("?")[0]
            );


        if(
            requested.startsWith(
                "/media/"
            )
        ){

            const file =
                path.join(
                    ROOT,
                    requested
                );


            if(
                file.startsWith(
                    MEDIA_DIR
                ) &&
                fs.existsSync(
                    file
                )
            ){

                const ext =
                    path.extname(
                        file
                    ).toLowerCase();


                let type =
                    "application/octet-stream";


                if(
                    ext === ".jpg" ||
                    ext === ".jpeg"
                ){

                    type =
                        "image/jpeg";

                }

                if(
                    ext === ".png"
                ){

                    type =
                        "image/png";

                }

                if(
                    ext === ".webp"
                ){

                    type =
                        "image/webp";

                }

                if(
                    ext === ".mp4"
                ){

                    type =
                        "video/mp4";

                }


                return send(

                    res,

                    200,

                    type,

                    fs.readFileSync(
                        file
                    )

                );

            }

        }

    }


    send(

        res,

        404,

        "text/plain",

        "Tidak ditemukan."

    );

});


server.listen(

    PORT,

    "127.0.0.1",

    ()=>{

        console.log("");
        console.log(
            "=============================="
        );
        console.log(
            "       ADMIN STOVENSA"
        );
        console.log(
            "=============================="
        );
        console.log(
            "http://127.0.0.1:" +
            PORT
        );
        console.log("");

    }

);
=== FILE: ./index_backup.html ===
<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stovensa</title>

    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: Arial, sans-serif;
            background: #f4f4f4;
            color: #222;
        }

        header {
            background: #0b3d91;
            color: white;
            padding: 20px;
            text-align: center;
        }

        header h1 {
            font-size: 30px;
        }

        header p {
            margin-top: 8px;
        }

        nav {
            background: #082c68;
            padding: 12px;
            text-align: center;
        }

        nav a {
            color: white;
            text-decoration: none;
            margin: 0 10px;
            font-weight: bold;
        }

        .hero {
            background: white;
            padding: 50px 20px;
            text-align: center;
        }

        .hero h2 {
            font-size: 32px;
            margin-bottom: 15px;
        }

        .hero p {
            font-size: 18px;
            color: #555;
        }

        .container {
            max-width: 1000px;
            margin: auto;
            padding: 25px 15px;
        }

        .section {
            background: white;
            margin-bottom: 20px;
            padding: 25px;
            border-radius: 10px;
        }

        .section h2 {
            margin-bottom: 15px;
            color: #0b3d91;
        }

        .video {
            width: 100%;
            max-height: 500px;
            background: #000;
            border-radius: 10px;
        }

        footer {
            background: #082c68;
            color: white;
            text-align: center;
            padding: 20px;
            margin-top: 20px;
        }
    </style>
</head>

<body>

<header>
    <h1>Stovensa</h1>
    <p>Website Resmi</p>
</header>

<nav>
    <a href="#">Beranda</a>
    <a href="#">Tentang</a>
    <a href="#">Berita</a>
    <a href="#">Galeri</a>
    <a href="#">Video</a>
</nav>

<section class="hero">
    <h2>Selamat Datang di Stovensa</h2>
    <p>Informasi, kegiatan, berita, foto, dan video.</p>
</section>

<div class="container">

    <section class="section">
        <h2>Tentang Stovensa</h2>
        <p>
            Ini adalah tempat untuk menampilkan informasi
            mengenai Stovensa. Teks ini nantinya bisa kita
            ubah sesuai kebutuhan.
        </p>
    </section>

    <section class="section">
        <h2>Berita Terbaru</h2>
        <p>
            Di bagian ini nantinya kita bisa menambahkan
            berita lengkap dengan foto dan tulisan.
        </p>
    </section>

    <section class="section">
        <h2>Video</h2>

        <video class="video" controls>
            <source src="video.mp4" type="video/mp4">
            Browser kamu tidak mendukung video.
        </video>
    </section>

</div>

<footer>
    <p>&copy; 2026 Stovensa</p>
</footer>

<!-- STOVENSA_DESKRIPSI_START -->
<p>The best generation</p>
<!-- STOVENSA_DESKRIPSI_END -->
</body>
</html>
=== FILE: ./admin_backup.html ===
<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Admin Stovensa</title>

    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background: #f1f3f6;
            color: #222;
        }

        header {
            background: #0b3d91;
            color: white;
            padding: 25px;
            text-align: center;
        }

        header h1 {
            margin: 0;
        }

        .container {
            max-width: 850px;
            margin: auto;
            padding: 20px;
        }

        .card {
            background: white;
            padding: 20px;
            margin-bottom: 20px;
            border-radius: 14px;
            box-shadow: 0 3px 12px rgba(0,0,0,.08);
        }

        h2 {
            margin-top: 0;
            color: #0b3d91;
        }

        label {
            display: block;
            margin-top: 15px;
            margin-bottom: 7px;
            font-weight: bold;
        }

        input,
        textarea {
            width: 100%;
            padding: 13px;
            border: 1px solid #ccc;
            border-radius: 8px;
            font-size: 16px;
        }

        textarea {
            min-height: 160px;
            resize: vertical;
        }

        input[type="file"] {
            padding: 10px;
        }

        .preview {
            display: none;
            margin-top: 15px;
            padding: 10px;
            background: #f5f5f5;
            border-radius: 10px;
        }

        .preview img,
        .preview video {
            max-width: 100%;
            max-height: 400px;
            border-radius: 8px;
        }

        button {
            width: 100%;
            padding: 14px;
            margin-top: 10px;
            border: none;
            border-radius: 8px;
            background: #0b3d91;
            color: white;
            font-size: 17px;
            font-weight: bold;
        }

        button:active {
            transform: scale(.99);
        }

        #status {
            margin-top: 15px;
            padding: 12px;
            border-radius: 8px;
            background: #eef3ff;
            display: none;
        }
    </style>
</head>

<body>

<header>
    <h1>Admin Stovensa</h1>
    <p>Kelola website Stovensa</p>
</header>

<div class="container">

    <div class="card">

        <h2>📝 Konten Website</h2>

        <label for="judul">
            Judul
        </label>

        <input
            type="text"
            id="judul"
            placeholder="Masukkan judul website">

        <label for="deskripsi">
            Deskripsi / Tulisan
        </label>

        <textarea
            id="deskripsi"
            placeholder="Masukkan tulisan website">
        </textarea>

    </div>


    <div class="card">

        <h2>🖼️ Foto</h2>

        <label for="foto">
            Pilih foto
        </label>

        <input
            type="file"
            id="foto"
            accept="image/*">

        <div
            class="preview"
            id="fotoPreview">

            <img
                id="fotoImage"
                alt="Preview foto">

        </div>

    </div>


    <div class="card">

        <h2>🎥 Video</h2>

        <label for="video">
            Pilih video
        </label>

        <input
            type="file"
            id="video"
            accept="video/*">

        <div
            class="preview"
            id="videoPreview">

            <video
                id="videoPlayer"
                controls>
            </video>

        </div>

    </div>


    <div class="card">

        <button onclick="simpanData()">
            💾 SIMPAN KE GITHUB
        </button>

        <div id="status"></div>

    </div>

</div>


<script>

const foto =
    document.getElementById("foto");

const video =
    document.getElementById("video");


/* PREVIEW FOTO */

foto.addEventListener("change", function () {

    const file = this.files[0];

    if (!file) return;

    const url =
        URL.createObjectURL(file);

    document.getElementById(
        "fotoImage"
    ).src = url;

    document.getElementById(
        "fotoPreview"
    ).style.display = "block";
});


/* PREVIEW VIDEO */

video.addEventListener("change", function () {

    const file = this.files[0];

    if (!file) return;

    const url =
        URL.createObjectURL(file);

    document.getElementById(
        "videoPlayer"
    ).src = url;

    document.getElementById(
        "videoPreview"
    ).style.display = "block";
});


/* SIMPAN KE SERVER */

async function simpanData() {

    const judul =
        document.getElementById(
            "judul"
        ).value;

    const deskripsi =
        document.getElementById(
            "deskripsi"
        ).value;

    const status =
        document.getElementById(
            "status"
        );

    status.style.display = "block";

    status.textContent =
        "⏳ Mengirim ke GitHub...";


    try {

        const response =
            await fetch(
                "/save",
                {
                    method: "POST",

                    headers: {
                        "Content-Type":
                            "application/json"
                    },

                    body: JSON.stringify({
                        judul: judul,
                        deskripsi: deskripsi
                    })
                }
            );


        const result =
            await response.json();


        if (result.success) {

            status.textContent =
                "✅ Berhasil dikirim ke GitHub!";

        } else {

            status.textContent =
                "❌ " +
                result.message;

            console.log(
                result.error
            );
        }


    } catch (error) {

        console.error(error);

        status.textContent =
            "❌ Server Termux tidak dapat dihubungi.";
    }

}

</script>

</body>
</html>
=== FILE: ./server_backup.js ===
const http = require("http");
const fs = require("fs");
const path = require("path");
const { execFile } = require("child_process");

const PORT = 3000;
const ROOT = __dirname;

function send(res, status, type, data) {
    res.writeHead(status, {
        "Content-Type": type + "; charset=utf-8"
    });
    res.end(data);
}

function runGit(args) {
    return new Promise((resolve, reject) => {
        execFile("git", args, { cwd: ROOT }, (error, stdout, stderr) => {
            if (error) {
                reject(stderr || error.message);
                return;
            }

            resolve(stdout);
        });
    });
}

const server = http.createServer((req, res) => {

    // =========================
    // MENAMPILKAN FILE
    // =========================

    if (req.method === "GET") {

        let file = req.url === "/"
            ? "admin.html"
            : req.url.substring(1);

        file = path.normalize(file);

        if (file.includes("..")) {
            return send(res, 403, "text/plain", "Forbidden");
        }

        const filePath = path.join(ROOT, file);

        fs.readFile(filePath, (err, data) => {

            if (err) {
                return send(
                    res,
                    404,
                    "text/plain",
                    "File tidak ditemukan"
                );
            }

            let type = "text/plain";

            if (file.endsWith(".html")) {
                type = "text/html";
            } else if (file.endsWith(".css")) {
                type = "text/css";
            } else if (file.endsWith(".js")) {
                type = "text/javascript";
            }

            send(res, 200, type, data);
        });

        return;
    }


    // =========================
    // MENYIMPAN DATA
    // =========================

    if (req.method === "POST" && req.url === "/save") {

        let body = "";

        req.on("data", chunk => {
            body += chunk;
        });

        req.on("end", async () => {

            try {

                const data = JSON.parse(body);

                const judul = String(data.judul || "");
                const deskripsi = String(data.deskripsi || "");

                const indexPath =
                    path.join(ROOT, "index.html");

                let html =
                    fs.readFileSync(indexPath, "utf8");


                // =========================
                // UBAH JUDUL YANG TERLIHAT
                // =========================

                const headingRegex =
                    /(<h1[^>]*>)([\s\S]*?)(<\/h1>)/i;

                if (headingRegex.test(html)) {

                    html = html.replace(
                        headingRegex,
                        `$1${judul}$3`
                    );

                } else {

                    throw new Error(
                        "Elemen <h1> tidak ditemukan."
                    );
                }


                // =========================
                // UBAH DESKRIPSI
                // =========================

                const markerStart =
                    "<!-- STOVENSA_DESKRIPSI_START -->";

                const markerEnd =
                    "<!-- STOVENSA_DESKRIPSI_END -->";

                if (
                    html.includes(markerStart) &&
                    html.includes(markerEnd)
                ) {

                    const descriptionRegex =
                        new RegExp(
                            markerStart +
                            "[\\s\\S]*?" +
                            markerEnd,
                            "i"
                        );

                    html = html.replace(
                        descriptionRegex,
                        markerStart +
                        "\n<p>" +
                        deskripsi +
                        "</p>\n" +
                        markerEnd
                    );

                } else {

                    html = html.replace(
                        /<\/body>/i,
                        markerStart +
                        "\n<p>" +
                        deskripsi +
                        "</p>\n" +
                        markerEnd +
                        "\n</body>"
                    );
                }


                // =========================
                // SIMPAN INDEX.HTML
                // =========================

                fs.writeFileSync(
                    indexPath,
                    html,
                    "utf8"
                );


                // =========================
                // GIT
                // =========================

                await runGit([
                    "add",
                    "index.html"
                ]);

                try {

                    await runGit([
                        "commit",
                        "-m",
                        "Update website dari Admin Stovensa"
                    ]);

                } catch (commitError) {

                    // Tidak ada perubahan untuk di-commit
                    if (
                        !String(commitError)
                            .includes("nothing to commit")
                    ) {
                        throw commitError;
                    }
                }


                // =========================
                // PUSH KE GITHUB
                // =========================

                await runGit([
                    "push",
                    "origin",
                    "main"
                ]);


                send(
                    res,
                    200,
                    "application/json",
                    JSON.stringify({
                        success: true,
                        message:
                            "Website berhasil diperbarui dan dikirim ke GitHub."
                    })
                );

            } catch (error) {

                console.error(
                    "ERROR:",
                    error
                );

                send(
                    res,
                    500,
                    "application/json",
                    JSON.stringify({
                        success: false,
                        message:
                            "Gagal memperbarui website.",
                        error:
                            String(error)
                    })
                );
            }
        });

        return;
    }


    send(
        res,
        404,
        "text/plain",
        "Not Found"
    );
});


server.listen(
    PORT,
    "127.0.0.1",
    () => {

        console.log("");
        console.log("==============================");
        console.log("       ADMIN STOVENSA");
        console.log("==============================");
        console.log(
            "Server: http://127.0.0.1:" + PORT
        );
        console.log("");
    }
);
=== FILE: ./server_backup2.js ===
const http = require("http");
const fs = require("fs");
const path = require("path");
const { execFile } = require("child_process");

const PORT = 3000;
const ROOT = __dirname;

function send(res, status, type, data) {
    res.writeHead(status, {
        "Content-Type": type + "; charset=utf-8"
    });
    res.end(data);
}

function runGit(args) {
    return new Promise((resolve, reject) => {
        execFile("git", args, { cwd: ROOT }, (error, stdout, stderr) => {
            if (error) {
                reject(stderr || error.message);
                return;
            }

            resolve(stdout);
        });
    });
}

const server = http.createServer((req, res) => {

    // =========================
    // MENAMPILKAN FILE
    // =========================

    if (req.method === "GET") {

        let file = req.url === "/"
            ? "admin.html"
            : req.url.substring(1);

        file = path.normalize(file);

        if (file.includes("..")) {
            return send(res, 403, "text/plain", "Forbidden");
        }

        const filePath = path.join(ROOT, file);

        fs.readFile(filePath, (err, data) => {

            if (err) {
                return send(
                    res,
                    404,
                    "text/plain",
                    "File tidak ditemukan"
                );
            }

            let type = "text/plain";

            if (file.endsWith(".html")) {
                type = "text/html";
            } else if (file.endsWith(".css")) {
                type = "text/css";
            } else if (file.endsWith(".js")) {
                type = "text/javascript";
            }

            send(res, 200, type, data);
        });

        return;
    }


    // =========================
    // MENYIMPAN DATA
    // =========================

    if (req.method === "POST" && req.url === "/save") {

        let body = "";

        req.on("data", chunk => {
            body += chunk;
        });

        req.on("end", async () => {

            try {

                const data = JSON.parse(body);

                const judul = String(data.judul || "");
                const deskripsi = String(data.deskripsi || "");

                const indexPath =
                    path.join(ROOT, "index.html");

                let html =
                    fs.readFileSync(indexPath, "utf8");


                // =========================
                // UBAH JUDUL YANG TERLIHAT
                // =========================

                const headingRegex =
                    /(<h1[^>]*>)([\s\S]*?)(<\/h1>)/i;

                if (headingRegex.test(html)) {

                    html = html.replace(
                        headingRegex,
                        `$1${judul}$3`
                    );

                } else {

                    throw new Error(
                        "Elemen <h1> tidak ditemukan."
                    );
                }


                // =========================
                // UBAH DESKRIPSI
                // =========================

                const markerStart =
                    "<!-- STOVENSA_DESKRIPSI_START -->";

                const markerEnd =
                    "<!-- STOVENSA_DESKRIPSI_END -->";

                if (
                    html.includes(markerStart) &&
                    html.includes(markerEnd)
                ) {

                    const descriptionRegex =
                        new RegExp(
                            markerStart +
                            "[\\s\\S]*?" +
                            markerEnd,
                            "i"
                        );

                    html = html.replace(
                        descriptionRegex,
                        markerStart +
                        "\n<p>" +
                        deskripsi +
                        "</p>\n" +
                        markerEnd
                    );

                } else {

                    html = html.replace(
                        /<\/body>/i,
                        markerStart +
                        "\n<p>" +
                        deskripsi +
                        "</p>\n" +
                        markerEnd +
                        "\n</body>"
                    );
                }


                // =========================
                // SIMPAN INDEX.HTML
                // =========================

                fs.writeFileSync(
                    indexPath,
                    html,
                    "utf8"
                );


                // =========================
                // GIT
                // =========================

                await runGit([
                    "add",
                    "index.html"
                ]);

                try {

                    await runGit([
                        "commit",
                        "-m",
                        "Update website dari Admin Stovensa"
                    ]);

                } catch (commitError) {

                    // Tidak ada perubahan untuk di-commit
                    if (
                        !String(commitError)
                            .includes("nothing to commit")
                    ) {
                        throw commitError;
                    }
                }


                // =========================
                // PUSH KE GITHUB
                // =========================

                await runGit([
                    "push",
                    "origin",
                    "main"
                ]);


                send(
                    res,
                    200,
                    "application/json",
                    JSON.stringify({
                        success: true,
                        message:
                            "Website berhasil diperbarui dan dikirim ke GitHub."
                    })
                );

            } catch (error) {

                console.error(
                    "ERROR:",
                    error
                );

                send(
                    res,
                    500,
                    "application/json",
                    JSON.stringify({
                        success: false,
                        message:
                            "Gagal memperbarui website.",
                        error:
                            String(error)
                    })
                );
            }
        });

        return;
    }


    send(
        res,
        404,
        "text/plain",
        "Not Found"
    );
});


server.listen(
    PORT,
    "127.0.0.1",
    () => {

        console.log("");
        console.log("==============================");
        console.log("       ADMIN STOVENSA");
        console.log("==============================");
        console.log(
            "Server: http://127.0.0.1:" + PORT
        );
        console.log("");
    }
);
=== FILE: ./admin_backup3.html ===
<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<title>Admin Stovensa</title>

<style>
* {
    box-sizing: border-box;
}

body {
    margin: 0;
    font-family: Arial, sans-serif;
    background: #f4f7fb;
    color: #172033;
}

header {
    background: linear-gradient(135deg, #1769ff, #7c3aed);
    color: white;
    padding: 28px 20px;
    text-align: center;
}

header h1 {
    margin: 0 0 8px;
}

.container {
    max-width: 850px;
    margin: auto;
    padding: 20px;
}

.card {
    background: white;
    padding: 22px;
    margin-bottom: 20px;
    border-radius: 18px;
    box-shadow: 0 8px 30px rgba(0,0,0,.08);
}

.card h2 {
    margin-top: 0;
    color: #1769ff;
}

label {
    display: block;
    font-weight: bold;
    margin: 15px 0 7px;
}

input,
textarea {
    width: 100%;
    padding: 13px;
    border: 1px solid #d5dbe5;
    border-radius: 10px;
    font-size: 16px;
    font-family: inherit;
}

textarea {
    min-height: 130px;
    resize: vertical;
}

input[type="file"] {
    background: #f8fafc;
}

button {
    width: 100%;
    padding: 15px;
    border: 0;
    border-radius: 12px;
    background: #1769ff;
    color: white;
    font-size: 17px;
    font-weight: bold;
    cursor: pointer;
}

button:hover {
    background: #0d55d9;
}

.preview {
    display: none;
    margin-top: 15px;
    padding: 10px;
    background: #f1f5f9;
    border-radius: 12px;
}

.preview img,
.preview video {
    display: block;
    width: 100%;
    max-height: 400px;
    object-fit: contain;
    border-radius: 10px;
}

#status {
    display: none;
    margin-top: 15px;
    padding: 13px;
    border-radius: 10px;
    background: #eef4ff;
}

.warning {
    font-size: 14px;
    color: #687386;
    line-height: 1.6;
}

@media (max-width: 600px) {
    .container {
        padding: 12px;
    }

    .card {
        padding: 18px;
    }
}
</style>
</head>

<body>

<header>
    <h1>⚙️ Admin Stovensa</h1>
    <p>Kelola isi website Stovensa</p>
</header>

<div class="container">

    <!-- JUDUL -->

    <div class="card">

        <h2>✏️ Judul Website</h2>

        <label for="judul">
            Judul utama
        </label>

        <input
            id="judul"
            type="text"
            placeholder="Contoh: STOVENSA">

    </div>


    <!-- DESKRIPSI -->

    <div class="card">

        <h2>📝 Deskripsi</h2>

        <label for="deskripsi">
            Deskripsi utama
        </label>

        <textarea
            id="deskripsi"
            placeholder="Tulis deskripsi Stovensa..."></textarea>

    </div>


    <!-- BERITA -->

    <div class="card">

        <h2>📰 Berita Terbaru</h2>

        <label for="beritaJudul">
            Judul berita
        </label>

        <input
            id="beritaJudul"
            type="text"
            placeholder="Judul berita">

        <label for="beritaIsi">
            Isi berita
        </label>

        <textarea
            id="beritaIsi"
            placeholder="Tuliskan berita..."></textarea>

    </div>


    <!-- FOTO -->

    <div class="card">

        <h2>📸 Foto</h2>

        <label for="foto">
            Pilih foto
        </label>

        <input
            id="foto"
            type="file"
            accept="image/*">

        <div
            class="preview"
            id="fotoPreview">

            <img
                id="fotoImage"
                alt="Preview foto">

        </div>

    </div>


    <!-- VIDEO -->

    <div class="card">

        <h2>🎬 Video</h2>

        <label for="video">
            Pilih video
        </label>

        <input
            id="video"
            type="file"
            accept="video/*">

        <div
            class="preview"
            id="videoPreview">

            <video
                id="videoPlayer"
                controls>
            </video>

        </div>

    </div>


    <!-- SIMPAN -->

    <div class="card">

        <button id="saveButton">
            💾 SIMPAN PERUBAHAN
        </button>

        <div id="status"></div>

        <p class="warning">
            Perubahan akan dikirim melalui server Termux
            kemudian diperbarui ke repository GitHub.
        </p>

    </div>

</div>


<script>

/* =========================
   PREVIEW FOTO
========================= */

const foto =
    document.getElementById("foto");

foto.addEventListener("change", function () {

    const file = this.files[0];

    if (!file) return;

    const url =
        URL.createObjectURL(file);

    document.getElementById(
        "fotoImage"
    ).src = url;

    document.getElementById(
        "fotoPreview"
    ).style.display = "block";
});


/* =========================
   PREVIEW VIDEO
========================= */

const video =
    document.getElementById("video");

video.addEventListener("change", function () {

    const file = this.files[0];

    if (!file) return;

    const url =
        URL.createObjectURL(file);

    document.getElementById(
        "videoPlayer"
    ).src = url;

    document.getElementById(
        "videoPreview"
    ).style.display = "block";
});


/* =========================
   SIMPAN
========================= */

document
.getElementById("saveButton")
.addEventListener("click", async function () {

    const status =
        document.getElementById("status");

    const judul =
        document.getElementById("judul").value;

    const deskripsi =
        document.getElementById("deskripsi").value;

    const beritaJudul =
        document.getElementById("beritaJudul").value;

    const beritaIsi =
        document.getElementById("beritaIsi").value;


    status.style.display = "block";

    status.textContent =
        "⏳ Mengirim perubahan...";


    try {

        const response =
            await fetch("/save", {

                method: "POST",

                headers: {
                    "Content-Type":
                        "application/json"
                },

                body: JSON.stringify({

                    judul: judul,

                    deskripsi: deskripsi,

                    beritaJudul:
                        beritaJudul,

                    beritaIsi:
                        beritaIsi

                })

            });


        const result =
            await response.json();


        if (result.success) {

            status.textContent =
                "✅ Perubahan berhasil dikirim ke GitHub.";

        } else {

            status.textContent =
                "❌ " +
                (result.message ||
                "Gagal menyimpan.");

            console.error(
                result.error
            );
        }


    } catch (error) {

        console.error(error);

        status.textContent =
            "❌ Server Termux tidak dapat dihubungi.";
    }

});

</script>

</body>
</html>
=== FILE: ./server_backup3.js ===
const http = require("http");
const fs = require("fs");
const path = require("path");
const { execFile } = require("child_process");

const PORT = 3000;
const ROOT = __dirname;

function runGit(args) {
    return new Promise((resolve, reject) => {
        execFile(
            "git",
            args,
            { cwd: ROOT },
            (error, stdout, stderr) => {

                if (error) {
                    reject(
                        stderr ||
                        stdout ||
                        error.message
                    );
                    return;
                }

                resolve(stdout);
            }
        );
    });
}

function sendJSON(res, status, data) {
    res.writeHead(status, {
        "Content-Type":
            "application/json; charset=utf-8"
    });

    res.end(
        JSON.stringify(data)
    );
}

function sendFile(res, filePath) {

    fs.readFile(
        filePath,
        (error, data) => {

            if (error) {
                res.writeHead(404);
                res.end("File tidak ditemukan.");
                return;
            }

            let type = "text/plain";

            if (
                filePath.endsWith(".html")
            ) {
                type = "text/html";
            }

            if (
                filePath.endsWith(".css")
            ) {
                type = "text/css";
            }

            if (
                filePath.endsWith(".js")
            ) {
                type = "text/javascript";
            }

            res.writeHead(200, {
                "Content-Type":
                    type + "; charset=utf-8"
            });

            res.end(data);
        }
    );
}


const server =
http.createServer(
    (req, res) => {

        /*
         * ADMIN
         */

        if (
            req.method === "GET" &&
            req.url === "/"
        ) {

            return sendFile(
                res,
                path.join(
                    ROOT,
                    "admin.html"
                )
            );
        }


        /*
         * SIMPAN DATA
         */

        if (
            req.method === "POST" &&
            req.url === "/save"
        ) {

            let body = "";

            req.on(
                "data",
                chunk => {
                    body += chunk;
                }
            );

            req.on(
                "end",
                async () => {

                    try {

                        const data =
                            JSON.parse(body);

                        const judul =
                            String(
                                data.judul || ""
                            );

                        const deskripsi =
                            String(
                                data.deskripsi || ""
                            );

                        const beritaJudul =
                            String(
                                data.beritaJudul || ""
                            );

                        const beritaIsi =
                            String(
                                data.beritaIsi || ""
                            );


                        /*
                         * BACA WEBSITE
                         */

                        const indexPath =
                            path.join(
                                ROOT,
                                "index.html"
                            );

                        let html =
                            fs.readFileSync(
                                indexPath,
                                "utf8"
                            );


                        /*
                         * JUDUL UTAMA
                         */

                        if (judul) {

                            const headingRegex =
                                /(<h1[^>]*>)([\s\S]*?)(<\/h1>)/i;

                            html =
                                html.replace(
                                    headingRegex,
                                    `$1${judul}$3`
                                );
                        }


                        /*
                         * DESKRIPSI
                         */

                        if (deskripsi) {

                            const markerStart =
                                "<!-- STOVENSA_DESKRIPSI_START -->";

                            const markerEnd =
                                "<!-- STOVENSA_DESKRIPSI_END -->";

                            const blockRegex =
                                new RegExp(
                                    markerStart +
                                    "[\\s\\S]*?" +
                                    markerEnd,
                                    "i"
                                );

                            const block =
                                markerStart +
                                "\n<p>" +
                                deskripsi +
                                "</p>\n" +
                                markerEnd;

                            if (
                                blockRegex.test(html)
                            ) {

                                html =
                                    html.replace(
                                        blockRegex,
                                        block
                                    );

                            } else {

                                html =
                                    html.replace(
                                        /<\/body>/i,
                                        block +
                                        "\n</body>"
                                    );
                            }
                        }


                        /*
                         * BERITA
                         */

                        if (
                            beritaJudul ||
                            beritaIsi
                        ) {

                            const berita =
                                `
<div class="card">
    <div class="icon">📰</div>
    <h3>${beritaJudul}</h3>
    <p>${beritaIsi}</p>
</div>
`;

                            const beritaMarker =
                                "<!-- STOVENSA_BERITA -->";

                            if (
                                html.includes(
                                    beritaMarker
                                )
                            ) {

                                html =
                                    html.replace(
                                        beritaMarker,
                                        beritaMarker +
                                        berita
                                    );

                            } else {

                                html =
                                    html.replace(
                                        "</section>",
                                        berita +
                                        "\n</section>"
                                    );
                            }
                        }


                        /*
                         * SIMPAN
                         */

                        fs.writeFileSync(
                            indexPath,
                            html,
                            "utf8"
                        );


                        /*
                         * GIT ADD
                         */

                        await runGit([
                            "add",
                            "index.html"
                        ]);


                        /*
                         * GIT COMMIT
                         */

                        try {

                            await runGit([
                                "commit",
                                "-m",
                                "Update Stovensa dari Admin"
                            ]);

                        } catch (commitError) {

                            const message =
                                String(
                                    commitError
                                );

                            if (
                                !message.includes(
                                    "nothing to commit"
                                )
                            ) {
                                throw commitError;
                            }
                        }


                        /*
                         * GIT PUSH
                         */

                        await runGit([
                            "push",
                            "origin",
                            "main"
                        ]);


                        sendJSON(
                            res,
                            200,
                            {
                                success: true,
                                message:
                                    "Berhasil memperbarui Stovensa."
                            }
                        );


                    } catch (error) {

                        console.error(
                            error
                        );

                        sendJSON(
                            res,
                            500,
                            {
                                success: false,
                                message:
                                    "Gagal memperbarui Stovensa.",
                                error:
                                    String(error)
                            }
                        );
                    }

                }
            );

            return;
        }


        /*
         * FILE LAIN
         */

        if (
            req.method === "GET"
        ) {

            let requested =
                req.url.split("?")[0];

            if (
                requested === "/"
            ) {
                requested =
                    "/admin.html";
            }

            const filePath =
                path.join(
                    ROOT,
                    requested
                );

            if (
                filePath.startsWith(ROOT)
            ) {

                return sendFile(
                    res,
                    filePath
                );
            }
        }


        res.writeHead(404);
        res.end(
            "Tidak ditemukan."
        );
    }
);


server.listen(
    PORT,
    "127.0.0.1",
    () => {

        console.log("");
        console.log(
            "=============================="
        );

        console.log(
            "      ADMIN STOVENSA"
        );

        console.log(
            "=============================="
        );

        console.log(
            "http://127.0.0.1:" +
            PORT
        );

        console.log("");
    }
);
=== FILE: ./index_backup3.html ===
<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<title>Stovensa</title>

<style>
* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}

html {
    scroll-behavior: smooth;
}

body {
    font-family: Arial, sans-serif;
    background: #f5f7fb;
    color: #172033;
    overflow-x: hidden;
}

/* NAVBAR */

nav {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    padding: 15px 6%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: rgba(255,255,255,.9);
    backdrop-filter: blur(12px);
    z-index: 1000;
    box-shadow: 0 2px 15px rgba(0,0,0,.08);
}

.logo {
    font-size: 24px;
    font-weight: 800;
    color: #1769ff;
}

nav a {
    text-decoration: none;
    color: #172033;
    margin-left: 20px;
    font-weight: 600;
}

nav a:hover {
    color: #1769ff;
}

/* HERO */

.hero {
    min-height: 100vh;
    padding: 140px 7% 80px;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    background:
        radial-gradient(circle at 20% 20%, #dce9ff, transparent 35%),
        radial-gradient(circle at 80% 70%, #d9f8ff, transparent 35%),
        #f5f7fb;
}

.hero-content {
    max-width: 850px;
    animation: heroIn 1s ease;
}

.hero h1 {
    font-size: clamp(48px, 10vw, 90px);
    font-weight: 900;
    background: linear-gradient(90deg,#1769ff,#8b5cf6,#00b8d9);
    -webkit-background-clip: text;
    color: transparent;
    margin-bottom: 20px;
}

.hero p {
    font-size: clamp(18px, 3vw, 25px);
    color: #596579;
    line-height: 1.7;
}

.hero-button {
    display: inline-block;
    margin-top: 30px;
    padding: 15px 28px;
    border-radius: 50px;
    background: #1769ff;
    color: white;
    text-decoration: none;
    font-weight: bold;
    box-shadow: 0 10px 30px rgba(23,105,255,.3);
    transition: .3s;
}

.hero-button:hover {
    transform: translateY(-5px) scale(1.03);
}

/* SECTION */

section {
    padding: 90px 7%;
}

.section-title {
    text-align: center;
    margin-bottom: 50px;
}

.section-title h2 {
    font-size: 38px;
    margin-bottom: 10px;
}

.section-title p {
    color: #687386;
}

/* CARDS */

.grid {
    display: grid;
    grid-template-columns:
        repeat(auto-fit, minmax(240px, 1fr));
    gap: 25px;
    max-width: 1100px;
    margin: auto;
}

.card {
    background: white;
    border-radius: 20px;
    padding: 30px;
    box-shadow: 0 10px 35px rgba(0,0,0,.07);
    transition: transform .3s, box-shadow .3s;
    opacity: 0;
    transform: translateY(40px);
}

.card.show {
    opacity: 1;
    transform: translateY(0);
}

.card:hover {
    transform: translateY(-8px);
    box-shadow: 0 18px 45px rgba(0,0,0,.12);
}

.icon {
    font-size: 42px;
    margin-bottom: 18px;
}

.card h3 {
    margin-bottom: 12px;
    font-size: 22px;
}

.card p {
    color: #687386;
    line-height: 1.7;
}

/* BERITA */

.news-card {
    overflow: hidden;
    padding: 0;
}

.news-image {
    height: 180px;
    background:
        linear-gradient(135deg,#1769ff,#8b5cf6);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 55px;
}

.news-content {
    padding: 25px;
}

/* VIDEO */

.video-box {
    max-width: 850px;
    margin: auto;
    background: white;
    padding: 15px;
    border-radius: 20px;
    box-shadow: 0 10px 35px rgba(0,0,0,.08);
}

.video-box video {
    width: 100%;
    border-radius: 14px;
}

/* TENTANG */

.about {
    max-width: 900px;
    margin: auto;
    background: white;
    padding: 40px;
    border-radius: 25px;
    box-shadow: 0 10px 35px rgba(0,0,0,.07);
}

.about p {
    line-height: 1.9;
    color: #687386;
    margin-top: 15px;
}

/* FOOTER */

footer {
    background: #111827;
    color: white;
    text-align: center;
    padding: 40px 20px;
}

footer p {
    color: #aeb7c7;
    margin-top: 10px;
}

/* ANIMATION */

@keyframes heroIn {
    from {
        opacity: 0;
        transform: translateY(35px);
    }

    to {
        opacity: 1;
        transform: translateY(0);
    }
}

/* MOBILE */

@media(max-width:700px) {

    nav {
        padding: 14px 5%;
    }

    nav a {
        margin-left: 8px;
        font-size: 13px;
    }

    .hero {
        padding-left: 6%;
        padding-right: 6%;
    }

    section {
        padding: 70px 5%;
    }

    .section-title h2 {
        font-size: 30px;
    }

    .about {
        padding: 25px;
    }
}
</style>
</head>

<body>

<!-- NAVBAR -->

<nav>

    <div class="logo">
        STOVENSA
    </div>

    <div>
        <a href="#beranda">Beranda</a>
        <a href="#berita">Berita</a>
        <a href="#galeri">Galeri</a>
        <a href="#tentang">Tentang</a>
    </div>

</nav>


<!-- HERO -->

<header class="hero" id="beranda">

    <div class="hero-content">

        <h1>Stovensa 7A</h1>

        <p>
            Website informasi, kegiatan,
            berita, foto, dan video Stovensa.
        </p>

        <a
            href="#berita"
            class="hero-button">
            Jelajahi Stovensa ↓
        </a>

    </div>

</header>


<!-- BERITA -->

<section id="berita">

    <div class="section-title">

        <h2>📰 Berita Terbaru</h2>

        <p>
            Informasi dan kegiatan terbaru Stovensa
        </p>

    </div>


    <div class="grid">

        <div class="card">

            <div class="icon">
                📢
            </div>

            <h3>
                Informasi Stovensa
            </h3>

            <p>
                Nantikan informasi dan kegiatan
                terbaru dari Stovensa.
            </p>

        </div>


        <div class="card">

            <div class="icon">
                🎉
            </div>

            <h3>
                Kegiatan
            </h3>

            <p>
                Berbagai kegiatan menarik
                akan ditampilkan di sini.
            </p>

        </div>


        <div class="card">

            <div class="icon">
                🏆
            </div>

            <h3>
                Prestasi
            </h3>

            <p>
                Tampilkan pencapaian dan
                prestasi terbaik Stovensa.
            </p>

        </div>

    </div>

</section>


<!-- GALERI -->

<section id="galeri">

    <div class="section-title">

        <h2>📸 Galeri</h2>

        <p>
            Dokumentasi foto Stovensa
        </p>

    </div>


    <div class="grid">

        <div class="card">

            <div class="icon">
                📷
            </div>

            <h3>
                Foto Kegiatan
            </h3>

            <p>
                Foto kegiatan akan
                ditampilkan di sini.
            </p>

        </div>


        <div class="card">

            <div class="icon">
                🌟
            </div>

            <h3>
                Momen Terbaik
            </h3>

            <p>
                Kumpulan momen terbaik
                Stovensa.
            </p>

        </div>

    </div>

</section>


<!-- VIDEO -->

<section>

    <div class="section-title">

        <h2>🎬 Video</h2>

        <p>
            Video kegiatan Stovensa
        </p>

    </div>


    <div class="video-box">

        <video controls>

            <p>
                Browser kamu tidak mendukung video.
            </p>

        </video>

    </div>

</section>


<!-- TENTANG -->

<section id="tentang">

    <div class="section-title">

        <h2>ℹ️ Tentang Stovensa</h2>

    </div>


    <div class="about">

        <p>
            Stovensa adalah tempat untuk
            berbagi informasi, kegiatan,
            berita, foto, dan video.
        </p>

        <p>
            Website ini dibuat agar informasi
            dapat diakses dengan mudah melalui
            perangkat HP maupun komputer.
        </p>

    </div>

</section>


<!-- FOOTER -->

<footer>

    <h3>
        STOVENSA
    </h3>

    <p>
        Website resmi Stovensa
    </p>

    <p>
        © 2026 Stovensa
    </p>

</footer>


<script>

/* ANIMASI SAAT SCROLL */

const cards =
    document.querySelectorAll(".card");


const observer =
    new IntersectionObserver(
        entries => {

            entries.forEach(
                entry => {

                    if (
                        entry.isIntersecting
                    ) {

                        entry.target.classList.add(
                            "show"
                        );

                    }

                }
            );

        },
        {
            threshold: 0.15
        }
    );


cards.forEach(
    card => observer.observe(card)
);

</script>

<!-- STOVENSA_DESKRIPSI_START -->
<p>Web percobaan</p>
<!-- STOVENSA_DESKRIPSI_END -->
</body>
</html>
=== FILE: ./index_backup_final.html ===
<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<title>Stovensa</title>

<style>
* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}

html {
    scroll-behavior: smooth;
}

body {
    font-family: Arial, sans-serif;
    background: #f5f7fb;
    color: #172033;
    overflow-x: hidden;
}

/* NAVBAR */

nav {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    padding: 15px 6%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: rgba(255,255,255,.9);
    backdrop-filter: blur(12px);
    z-index: 1000;
    box-shadow: 0 2px 15px rgba(0,0,0,.08);
}

.logo {
    font-size: 24px;
    font-weight: 800;
    color: #1769ff;
}

nav a {
    text-decoration: none;
    color: #172033;
    margin-left: 20px;
    font-weight: 600;
}

nav a:hover {
    color: #1769ff;
}

/* HERO */

.hero {
    min-height: 100vh;
    padding: 140px 7% 80px;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    background:
        radial-gradient(circle at 20% 20%, #dce9ff, transparent 35%),
        radial-gradient(circle at 80% 70%, #d9f8ff, transparent 35%),
        #f5f7fb;
}

.hero-content {
    max-width: 850px;
    animation: heroIn 1s ease;
}

.hero h1 {
    font-size: clamp(48px, 10vw, 90px);
    font-weight: 900;
    background: linear-gradient(90deg,#1769ff,#8b5cf6,#00b8d9);
    -webkit-background-clip: text;
    color: transparent;
    margin-bottom: 20px;
}

.hero p {
    font-size: clamp(18px, 3vw, 25px);
    color: #596579;
    line-height: 1.7;
}

.hero-button {
    display: inline-block;
    margin-top: 30px;
    padding: 15px 28px;
    border-radius: 50px;
    background: #1769ff;
    color: white;
    text-decoration: none;
    font-weight: bold;
    box-shadow: 0 10px 30px rgba(23,105,255,.3);
    transition: .3s;
}

.hero-button:hover {
    transform: translateY(-5px) scale(1.03);
}

/* SECTION */

section {
    padding: 90px 7%;
}

.section-title {
    text-align: center;
    margin-bottom: 50px;
}

.section-title h2 {
    font-size: 38px;
    margin-bottom: 10px;
}

.section-title p {
    color: #687386;
}

/* CARDS */

.grid {
    display: grid;
    grid-template-columns:
        repeat(auto-fit, minmax(240px, 1fr));
    gap: 25px;
    max-width: 1100px;
    margin: auto;
}

.card {
    background: white;
    border-radius: 20px;
    padding: 30px;
    box-shadow: 0 10px 35px rgba(0,0,0,.07);
    transition: transform .3s, box-shadow .3s;
    opacity: 0;
    transform: translateY(40px);
}

.card.show {
    opacity: 1;
    transform: translateY(0);
}

.card:hover {
    transform: translateY(-8px);
    box-shadow: 0 18px 45px rgba(0,0,0,.12);
}

.icon {
    font-size: 42px;
    margin-bottom: 18px;
}

.card h3 {
    margin-bottom: 12px;
    font-size: 22px;
}

.card p {
    color: #687386;
    line-height: 1.7;
}

/* BERITA */

.news-card {
    overflow: hidden;
    padding: 0;
}

.news-image {
    height: 180px;
    background:
        linear-gradient(135deg,#1769ff,#8b5cf6);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 55px;
}

.news-content {
    padding: 25px;
}

/* VIDEO */

.video-box {
    max-width: 850px;
    margin: auto;
    background: white;
    padding: 15px;
    border-radius: 20px;
    box-shadow: 0 10px 35px rgba(0,0,0,.08);
}

.video-box video {
    width: 100%;
    border-radius: 14px;
}

/* TENTANG */

.about {
    max-width: 900px;
    margin: auto;
    background: white;
    padding: 40px;
    border-radius: 25px;
    box-shadow: 0 10px 35px rgba(0,0,0,.07);
}

.about p {
    line-height: 1.9;
    color: #687386;
    margin-top: 15px;
}

/* FOOTER */

footer {
    background: #111827;
    color: white;
    text-align: center;
    padding: 40px 20px;
}

footer p {
    color: #aeb7c7;
    margin-top: 10px;
}

/* ANIMATION */

@keyframes heroIn {
    from {
        opacity: 0;
        transform: translateY(35px);
    }

    to {
        opacity: 1;
        transform: translateY(0);
    }
}

/* MOBILE */

@media(max-width:700px) {

    nav {
        padding: 14px 5%;
    }

    nav a {
        margin-left: 8px;
        font-size: 13px;
    }

    .hero {
        padding-left: 6%;
        padding-right: 6%;
    }

    section {
        padding: 70px 5%;
    }

    .section-title h2 {
        font-size: 30px;
    }

    .about {
        padding: 25px;
    }
}
</style>
</head>

<body>

<!-- NAVBAR -->

<nav>

    <div class="logo">
        STOVENSA
    </div>

    <div>
        <a href="#beranda">Beranda</a>
        <a href="#berita">Berita</a>
        <a href="#galeri">Galeri</a>
        <a href="#tentang">Tentang</a>
    </div>

</nav>


<!-- HERO -->

<header class="hero" id="beranda">

    <div class="hero-content">

        <h1>Stovensa 7A</h1>

        <p>
            Website informasi, kegiatan,
            berita, foto, dan video Stovensa.
        </p>

        <a
            href="#berita"
            class="hero-button">
            Jelajahi Stovensa ↓
        </a>

    </div>

</header>


<!-- BERITA -->

<section id="berita">

    <div class="section-title">

        <h2>📰 Berita Terbaru</h2>

        <p>
            Informasi dan kegiatan terbaru Stovensa
        </p>

    </div>


    <div class="grid">

        <div class="card">

            <div class="icon">
                📢
            </div>

            <h3>
                Informasi Stovensa
            </h3>

            <p>
                Nantikan informasi dan kegiatan
                terbaru dari Stovensa.
            </p>

        </div>


        <div class="card">

            <div class="icon">
                🎉
            </div>

            <h3>
                Kegiatan
            </h3>

            <p>
                Berbagai kegiatan menarik
                akan ditampilkan di sini.
            </p>

        </div>


        <div class="card">

            <div class="icon">
                🏆
            </div>

            <h3>
                Prestasi
            </h3>

            <p>
                Tampilkan pencapaian dan
                prestasi terbaik Stovensa.
            </p>

        </div>

    </div>

</section>


<!-- GALERI -->

<section id="galeri">

    <div class="section-title">

        <h2>📸 Galeri</h2>

        <p>
            Dokumentasi foto Stovensa
        </p>

    </div>


    <div class="grid">

        <div class="card">

            <div class="icon">
                📷
            </div>

            <h3>
                Foto Kegiatan
            </h3>

            <p>
                Foto kegiatan akan
                ditampilkan di sini.
            </p>

        </div>


        <div class="card">

            <div class="icon">
                🌟
            </div>

            <h3>
                Momen Terbaik
            </h3>

            <p>
                Kumpulan momen terbaik
                Stovensa.
            </p>

        </div>

    </div>

</section>


<!-- VIDEO -->

<section>

    <div class="section-title">

        <h2>🎬 Video</h2>

        <p>
            Video kegiatan Stovensa
        </p>

    </div>


    <div class="video-box">

        <video controls>

            <p>
                Browser kamu tidak mendukung video.
            </p>

        </video>

    </div>

</section>


<!-- TENTANG -->

<section id="tentang">

    <div class="section-title">

        <h2>ℹ️ Tentang Stovensa</h2>

    </div>


    <div class="about">

        <p>
            Stovensa adalah tempat untuk
            berbagi informasi, kegiatan,
            berita, foto, dan video.
        </p>

        <p>
            Website ini dibuat agar informasi
            dapat diakses dengan mudah melalui
            perangkat HP maupun komputer.
        </p>

    </div>

</section>


<!-- FOOTER -->

<footer>

    <h3>
        STOVENSA
    </h3>

    <p>
        Website resmi Stovensa
    </p>

    <p>
        © 2026 Stovensa
    </p>

</footer>


<script>

/* ANIMASI SAAT SCROLL */

const cards =
    document.querySelectorAll(".card");


const observer =
    new IntersectionObserver(
        entries => {

            entries.forEach(
                entry => {

                    if (
                        entry.isIntersecting
                    ) {

                        entry.target.classList.add(
                            "show"
                        );

                    }

                }
            );

        },
        {
            threshold: 0.15
        }
    );


cards.forEach(
    card => observer.observe(card)
);

</script>

<!-- STOVENSA_DESKRIPSI_START -->
<p>Web percobaan</p>
<!-- STOVENSA_DESKRIPSI_END -->
</body>
</html>
=== FILE: ./admin_backup_final.html ===
<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<title>Admin Stovensa</title>

<style>
* {
    box-sizing: border-box;
}

body {
    margin: 0;
    font-family: Arial, sans-serif;
    background: #f4f7fb;
    color: #172033;
}

header {
    background: linear-gradient(135deg, #1769ff, #7c3aed);
    color: white;
    padding: 28px 20px;
    text-align: center;
}

header h1 {
    margin: 0 0 8px;
}

.container {
    max-width: 850px;
    margin: auto;
    padding: 20px;
}

.card {
    background: white;
    padding: 22px;
    margin-bottom: 20px;
    border-radius: 18px;
    box-shadow: 0 8px 30px rgba(0,0,0,.08);
}

.card h2 {
    margin-top: 0;
    color: #1769ff;
}

label {
    display: block;
    font-weight: bold;
    margin: 15px 0 7px;
}

input,
textarea {
    width: 100%;
    padding: 13px;
    border: 1px solid #d5dbe5;
    border-radius: 10px;
    font-size: 16px;
    font-family: inherit;
}

textarea {
    min-height: 130px;
    resize: vertical;
}

input[type="file"] {
    background: #f8fafc;
}

button {
    width: 100%;
    padding: 15px;
    border: 0;
    border-radius: 12px;
    background: #1769ff;
    color: white;
    font-size: 17px;
    font-weight: bold;
    cursor: pointer;
}

button:hover {
    background: #0d55d9;
}

.preview {
    display: none;
    margin-top: 15px;
    padding: 10px;
    background: #f1f5f9;
    border-radius: 12px;
}

.preview img,
.preview video {
    display: block;
    width: 100%;
    max-height: 400px;
    object-fit: contain;
    border-radius: 10px;
}

#status {
    display: none;
    margin-top: 15px;
    padding: 13px;
    border-radius: 10px;
    background: #eef4ff;
}

.warning {
    font-size: 14px;
    color: #687386;
    line-height: 1.6;
}

@media (max-width: 600px) {
    .container {
        padding: 12px;
    }

    .card {
        padding: 18px;
    }
}
</style>
</head>

<body>

<header>
    <h1>⚙️ Admin Stovensa</h1>
    <p>Kelola isi website Stovensa</p>
</header>

<div class="container">

    <!-- JUDUL -->

    <div class="card">

        <h2>✏️ Judul Website</h2>

        <label for="judul">
            Judul utama
        </label>

        <input
            id="judul"
            type="text"
            placeholder="Contoh: STOVENSA">

    </div>


    <!-- DESKRIPSI -->

    <div class="card">

        <h2>📝 Deskripsi</h2>

        <label for="deskripsi">
            Deskripsi utama
        </label>

        <textarea
            id="deskripsi"
            placeholder="Tulis deskripsi Stovensa..."></textarea>

    </div>


    <!-- BERITA -->

    <div class="card">

        <h2>📰 Berita Terbaru</h2>

        <label for="beritaJudul">
            Judul berita
        </label>

        <input
            id="beritaJudul"
            type="text"
            placeholder="Judul berita">

        <label for="beritaIsi">
            Isi berita
        </label>

        <textarea
            id="beritaIsi"
            placeholder="Tuliskan berita..."></textarea>

    </div>


    <!-- FOTO -->

    <div class="card">

        <h2>📸 Foto</h2>

        <label for="foto">
            Pilih foto
        </label>

        <input
            id="foto"
            type="file"
            accept="image/*">

        <div
            class="preview"
            id="fotoPreview">

            <img
                id="fotoImage"
                alt="Preview foto">

        </div>

    </div>


    <!-- VIDEO -->

    <div class="card">

        <h2>🎬 Video</h2>

        <label for="video">
            Pilih video
        </label>

        <input
            id="video"
            type="file"
            accept="video/*">

        <div
            class="preview"
            id="videoPreview">

            <video
                id="videoPlayer"
                controls>
            </video>

        </div>

    </div>


    <!-- SIMPAN -->

    <div class="card">

        <button id="saveButton">
            💾 SIMPAN PERUBAHAN
        </button>

        <div id="status"></div>

        <p class="warning">
            Perubahan akan dikirim melalui server Termux
            kemudian diperbarui ke repository GitHub.
        </p>

    </div>

</div>


<script>

/* =========================
   PREVIEW FOTO
========================= */

const foto =
    document.getElementById("foto");

foto.addEventListener("change", function () {

    const file = this.files[0];

    if (!file) return;

    const url =
        URL.createObjectURL(file);

    document.getElementById(
        "fotoImage"
    ).src = url;

    document.getElementById(
        "fotoPreview"
    ).style.display = "block";
});


/* =========================
   PREVIEW VIDEO
========================= */

const video =
    document.getElementById("video");

video.addEventListener("change", function () {

    const file = this.files[0];

    if (!file) return;

    const url =
        URL.createObjectURL(file);

    document.getElementById(
        "videoPlayer"
    ).src = url;

    document.getElementById(
        "videoPreview"
    ).style.display = "block";
});


/* =========================
   SIMPAN
========================= */

document
.getElementById("saveButton")
.addEventListener("click", async function () {

    const status =
        document.getElementById("status");

    const judul =
        document.getElementById("judul").value;

    const deskripsi =
        document.getElementById("deskripsi").value;

    const beritaJudul =
        document.getElementById("beritaJudul").value;

    const beritaIsi =
        document.getElementById("beritaIsi").value;


    status.style.display = "block";

    status.textContent =
        "⏳ Mengirim perubahan...";


    try {

        const response =
            await fetch("/save", {

                method: "POST",

                headers: {
                    "Content-Type":
                        "application/json"
                },

                body: JSON.stringify({

                    judul: judul,

                    deskripsi: deskripsi,

                    beritaJudul:
                        beritaJudul,

                    beritaIsi:
                        beritaIsi

                })

            });


        const result =
            await response.json();


        if (result.success) {

            status.textContent =
                "✅ Perubahan berhasil dikirim ke GitHub.";

        } else {

            status.textContent =
                "❌ " +
                (result.message ||
                "Gagal menyimpan.");

            console.error(
                result.error
            );
        }


    } catch (error) {

        console.error(error);

        status.textContent =
            "❌ Server Termux tidak dapat dihubungi.";
    }

});

</script>

</body>
</html>
=== FILE: ./server_backup_final.js ===
const http = require("http");
const fs = require("fs");
const path = require("path");
const { execFile } = require("child_process");

const PORT = 3000;
const ROOT = __dirname;
const DATA_DIR = path.join(ROOT, "data");
const MEDIA_DIR = path.join(ROOT, "media");
const DATA_FILE = path.join(DATA_DIR, "site.json");
const INDEX_FILE = path.join(ROOT, "index.html");

fs.mkdirSync(DATA_DIR, { recursive: true });
fs.mkdirSync(MEDIA_DIR, { recursive: true });

function git(args) {
    return new Promise((resolve, reject) => {
        execFile("git", args, { cwd: ROOT }, (err, stdout, stderr) => {
            if (err) {
                reject(stderr || stdout || err.message);
                return;
            }
            resolve(stdout);
        });
    });
}

function send(res, code, type, data) {
    res.writeHead(code, {
        "Content-Type": type + "; charset=utf-8"
    });
    res.end(data);
}

function json(res, code, data) {
    send(
        res,
        code,
        "application/json",
        JSON.stringify(data)
    );
}

function escapeHTML(text) {
    return String(text || "")
        .replace(/&/g, "&amp;")
        .replace(/</g, "&lt;")
        .replace(/>/g, "&gt;")
        .replace(/"/g, "&quot;")
        .replace(/'/g, "&#039;");
}

function loadData() {
    if (!fs.existsSync(DATA_FILE)) {
        return {
            judul: "STOVENSA",
            deskripsi: "Website informasi Stovensa.",
            berita: [],
            foto: [],
            video: []
        };
    }

    try {
        return JSON.parse(
            fs.readFileSync(DATA_FILE, "utf8")
        );
    } catch {
        return {
            judul: "STOVENSA",
            deskripsi: "Website informasi Stovensa.",
            berita: [],
            foto: [],
            video: []
        };
    }
}

function saveData(data) {
    fs.writeFileSync(
        DATA_FILE,
        JSON.stringify(data, null, 2),
        "utf8"
    );
}

function createWebsite(data) {

    let html = fs.readFileSync(
        INDEX_FILE,
        "utf8"
    );

    const title = escapeHTML(data.judul);
    const description = escapeHTML(data.deskripsi);

    html = html.replace(
        /(<title>)([\s\S]*?)(<\/title>)/i,
        `$1${title}$3`
    );

    html = html.replace(
        /(<h1[^>]*>)([\s\S]*?)(<\/h1>)/i,
        `$1${title}$3`
    );

    html = html.replace(
        /<!-- STOVENSA_DESKRIPSI_START -->[\s\S]*?<!-- STOVENSA_DESKRIPSI_END -->/i,
        `<!-- STOVENSA_DESKRIPSI_START -->
<p>${description}</p>
<!-- STOVENSA_DESKRIPSI_END -->`
    );

    const beritaHTML = data.berita.map(item => `
        <article class="card news-card">
            <div class="news-content">
                <h3>${escapeHTML(item.judul)}</h3>
                <p>${escapeHTML(item.isi)}</p>
            </div>
        </article>
    `).join("");

    const fotoHTML = data.foto.map(item => `
        <article class="card media-card">
            <img
                src="${escapeHTML(item.url)}"
                alt="${escapeHTML(item.nama)}">
        </article>
    `).join("");

    const videoHTML = data.video.map(item => `
        <article class="card media-card">
            <video controls preload="metadata">
                <source
                    src="${escapeHTML(item.url)}">
            </video>
        </article>
    `).join("");

    html = html.replace(
        /<!-- STOVENSA_BERITA_START -->[\s\S]*?<!-- STOVENSA_BERITA_END -->/i,
        `<!-- STOVENSA_BERITA_START -->
${beritaHTML || `
<div class="card">
    <div class="icon">📰</div>
    <h3>Belum ada berita</h3>
    <p>Berita terbaru akan muncul di sini.</p>
</div>
`}
<!-- STOVENSA_BERITA_END -->`
    );

    html = html.replace(
        /<!-- STOVENSA_FOTO_START -->[\s\S]*?<!-- STOVENSA_FOTO_END -->/i,
        `<!-- STOVENSA_FOTO_START -->
${fotoHTML || `
<div class="card">
    <div class="icon">📸</div>
    <h3>Belum ada foto</h3>
    <p>Foto kegiatan akan muncul di sini.</p>
</div>
`}
<!-- STOVENSA_FOTO_END -->`
    );

    html = html.replace(
        /<!-- STOVENSA_VIDEO_START -->[\s\S]*?<!-- STOVENSA_VIDEO_END -->/i,
        `<!-- STOVENSA_VIDEO_START -->
${videoHTML || `
<div class="card">
    <div class="icon">🎬</div>
    <h3>Belum ada video</h3>
    <p>Video kegiatan akan muncul di sini.</p>
</div>
`}
<!-- STOVENSA_VIDEO_END -->`
    );

    fs.writeFileSync(
        INDEX_FILE,
        html,
        "utf8"
    );
}

async function updateGitHub() {

    await git([
        "add",
        "index.html",
        "data/site.json",
        "media"
    ]);

    try {
        await git([
            "commit",
            "-m",
            "Update Stovensa dari Admin"
        ]);
    } catch (e) {
        if (!String(e).includes("nothing to commit")) {
            throw e;
        }
    }

    await git([
        "push",
        "origin",
        "main"
    ]);
}

function receiveBody(req) {
    return new Promise((resolve, reject) => {

        let body = "";

        req.on("data", chunk => {
            body += chunk;

            if (body.length > 150 * 1024 * 1024) {
                reject(
                    new Error("File terlalu besar.")
                );
                req.destroy();
            }
        });

        req.on("end", () => {
            try {
                resolve(JSON.parse(body));
            } catch {
                reject(
                    new Error("Data JSON tidak valid.")
                );
            }
        });

        req.on("error", reject);
    });
}

const server = http.createServer(
    async (req, res) => {

        if (
            req.method === "GET" &&
            req.url === "/"
        ) {
            return send(
                res,
                200,
                "text/html",
                fs.readFileSync(
                    path.join(ROOT, "admin.html")
                )
            );
        }

        if (
            req.method === "GET" &&
            req.url === "/api/data"
        ) {
            return json(
                res,
                200,
                loadData()
            );
        }

        if (
            req.method === "POST" &&
            req.url === "/api/save"
        ) {

            try {

                const input =
                    await receiveBody(req);

                const data = loadData();

                if (typeof input.judul === "string") {
                    data.judul = input.judul;
                }

                if (typeof input.deskripsi === "string") {
                    data.deskripsi = input.deskripsi;
                }

                if (
                    input.aksi === "tambah_berita"
                ) {

                    data.berita.unshift({
                        id: Date.now(),
                        judul: input.beritaJudul || "",
                        isi: input.beritaIsi || ""
                    });
                }

                if (
                    input.aksi === "hapus_berita"
                ) {

                    data.berita =
                        data.berita.filter(
                            item =>
                                String(item.id) !==
                                String(input.id)
                        );
                }

                if (
                    input.aksi === "upload_media"
                ) {

                    const base64 =
                        String(input.data || "");

                    const type =
                        input.type === "video"
                            ? "video"
                            : "image";

                    const extension =
                        type === "video"
                            ? ".mp4"
                            : ".jpg";

                    const filename =
                        Date.now() +
                        "_" +
                        Math.random()
                            .toString(36)
                            .slice(2, 8) +
                        extension;

                    const relative =
                        "media/" + filename;

                    const output =
                        path.join(
                            ROOT,
                            relative
                        );

                    const cleanBase64 =
                        base64.includes(",")
                            ? base64.split(",")[1]
                            : base64;

                    fs.writeFileSync(
                        output,
                        Buffer.from(
                            cleanBase64,
                            "base64"
                        )
                    );

                    const item = {
                        id: Date.now(),
                        nama:
                            input.nama ||
                            filename,
                        url: relative
                    };

                    if (type === "video") {
                        data.video.unshift(item);
                    } else {
                        data.foto.unshift(item);
                    }
                }

                if (
                    input.aksi === "hapus_foto"
                ) {

                    const item =
                        data.foto.find(
                            x =>
                                String(x.id) ===
                                String(input.id)
                        );

                    if (item) {

                        const file =
                            path.join(
                                ROOT,
                                item.url
                            );

                        if (fs.existsSync(file)) {
                            fs.unlinkSync(file);
                        }
                    }

                    data.foto =
                        data.foto.filter(
                            x =>
                                String(x.id) !==
                                String(input.id)
                        );
                }

                if (
                    input.aksi === "hapus_video"
                ) {

                    const item =
                        data.video.find(
                            x =>
                                String(x.id) ===
                                String(input.id)
                        );

                    if (item) {

                        const file =
                            path.join(
                                ROOT,
                                item.url
                            );

                        if (fs.existsSync(file)) {
                            fs.unlinkSync(file);
                        }
                    }

                    data.video =
                        data.video.filter(
                            x =>
                                String(x.id) !==
                                String(input.id)
                        );
                }

                saveData(data);
                createWebsite(data);
                await updateGitHub();

                return json(
                    res,
                    200,
                    {
                        success: true,
                        data: data,
                        message:
                            "Berhasil disimpan ke GitHub."
                    }
                );

            } catch (error) {

                console.error(error);

                return json(
                    res,
                    500,
                    {
                        success: false,
                        message:
                            String(error)
                    }
                );
            }
        }

        if (req.method === "GET") {

            let requested =
                decodeURIComponent(
                    req.url.split("?")[0]
                );

            if (requested === "/") {
                requested = "/admin.html";
            }

            const filePath =
                path.join(
                    ROOT,
                    requested
                );

            if (
                filePath.startsWith(ROOT) &&
                fs.existsSync(filePath) &&
                fs.statSync(filePath).isFile()
            ) {
                return send(
                    res,
                    200,
                    "text/plain",
                    fs.readFileSync(filePath)
                );
            }
        }

        send(
            res,
            404,
            "text/plain",
            "Tidak ditemukan."
        );
    }
);

server.listen(
    PORT,
    "127.0.0.1",
    () => {
        console.log(
            "Admin Stovensa: http://127.0.0.1:" +
            PORT
        );
    }
);
